// Here I created a one line function (ie. an arrow function) that receives two numbers as parameters
// and returns the sum.
const sum = (a, b) => a + b;

// Example usage:

console.log(sum(5, 3));    // 8
console.log(sum(-2, 7));   // 5
console.log(sum(1.5, 2.5)); // 4